<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12 replaced the launch drawable with the system SplashScreen API:
    from API 31 the platform draws the app icon on a background colour and
    ignores the window background, so the artwork in drawable/splash.png is
    never shown on a modern device. Setting the background to the artwork's own
    ground colour keeps the launch dark and continuous instead of flashing pale
    before the app appears.
-->
<resources>
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>
